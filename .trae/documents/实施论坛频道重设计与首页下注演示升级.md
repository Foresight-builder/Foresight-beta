## 改造范围与目标
- 统一主视觉面板与按钮色系：所有关键 div 面板应用与按钮一致的浅色渐变，避免割裂并提升信息密度。
- 论坛频道重设计：采用“渐变卡片栅格 + 图标 + 统计 + CTA”，更强层次与交互反馈。
- 首页聊天/论坛展示真实化：展示最近 3 条消息与热门主题缩略，双主 CTA 保留并强化转化。
- 下注演示升级：参考 Polymarket，新增折线图与交互，支持二元、多元（3 结果）、区间三形态。

## 样式与可访问性
- 新增面板类：`panel-primary/secondary/success/warning/danger` 与 `panel-mint/azure/lilac/peach`（与按钮系统一致）。
- 统一圆角/阴影/间距：`rounded-2xl`、`shadow-sm|md`、8px 间距阶梯。
- 文本对比度校准：浅背景统一深色/白色文本，满足 WCAG 2.1 AA；焦点环统一可见。

## 文件改动
- `apps/web/src/app/globals.css`：新增 `panel-*` 工具类与浅背景装饰类，补充无障碍焦点环。
- `apps/web/src/components/ForumSection.tsx`：头部与按钮改用统一面板/按钮类；投票与发帖交互一致。
- `apps/web/src/app/forum/page.tsx`：
  - 左侧频道改为渐变卡片栅格（4 色方案），含图标/简介/主题总数/今日新增/入口按钮；Chip 统一渐变。
  - 顶部说明条采用浅品牌色，增强密度与引导。
- `apps/web/src/app/page.tsx`：
  - 新增聊天预览模块（最近 3 条消息 + 进入按钮）、论坛热门缩略完善。
  - 下注演示区新增多元与区间两个演示框；折线图使用 Canvas/SVG 自绘；保留双主 CTA。
- 可选小组件（就地实现，避免文件过多）：`ChartLine`（Canvas/SVG）、`ChannelCard`（论坛频道卡）。

## 交互与无障碍验收
- 主视觉面板与按钮色系一致；卡片 hover 阴影/轻微缩放；键盘焦点环可见。
- 首页预览内容真实且可导航；论坛频道信息密度提升、CTA 明确。
- 下注演示具备折线图与三形态演示，交互顺畅；AA 对比度达标，键盘导航完整。

## 实施顺序
1. 新增 `panel-*` 样式令牌并应用到论坛/首页关键面板。
2. 重绘论坛频道栅格与 Chip。
3. 增强首页聊天/论坛预览模块。
4. 扩展下注演示到二元/多元/区间并接入折线图与交互。
5. 无障碍与细节收尾（对比度、焦点环、空态与骨架）。

确认后立即按以上步骤实施并提供预览链接与代码位置说明。